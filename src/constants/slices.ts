export enum SLICES {
  AUTH = 'AUTH',
  USER = 'USER',
  PHOTO = 'PHOTO',
}

export enum AUTH {
  SET_AUTH = 'SET_AUTH',
}

export enum USER {
  SET_USER = 'SET_USER',
}

export enum PHOTO {
  SET_PHOTO = 'SET_PHOTO',
}
