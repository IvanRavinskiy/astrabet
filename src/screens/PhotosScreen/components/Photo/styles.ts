import {StyleSheet} from 'react-native';

export const photoStyles = StyleSheet.create({
  root: {
    alignItems: 'center',
    marginVertical: 20,
  },
  photo: {
    width: 300,
    height: 300,
  },
});
